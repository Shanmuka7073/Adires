'use client';

/**
 * PROFILE COMPLETION CHECKER (DISABLED)
 * Profile completion is no longer mandatory. Returning null to prevent popup.
 */
export function ProfileCompletionChecker() {
  return null;
}
