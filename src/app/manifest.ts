
/**
 * Static Manifest Generator (DEPRECATED)
 * This file is being removed to prevent conflicts with the dynamic store-specific manifests.
 * Refer to src/app/manifest/[storeId]/route.ts for the actual implementation.
 */
export default function manifest() {
  return null;
}
