'use client';

/**
 * PROMPT CONTENT (DECOMMISSIONED)
 * Exporting a stub to prevent build errors if referenced.
 */
export function generateSupportPrompt() {
  return "System prompt generator has been decommissioned.";
}
