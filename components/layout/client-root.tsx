'use client';

/**
 * ROOT LEVEL PROXY
 * Delegates to the consolidated @/components folder.
 */

import { ClientRoot as ClientRootInternal } from '@/components/layout/client-root';

export const ClientRoot = ClientRootInternal;
